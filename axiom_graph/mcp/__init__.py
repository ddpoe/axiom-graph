"""Axiom-graph MCP tool implementations, organized by concern.

Sub-modules:
    _helpers    -- shared utilities (DB access, section-ID parsing, doc I/O)
    query       -- read-only index query tools
    doc         -- document manipulation tools
    lifecycle   -- build, staleness, verification, and history tools
    workflows   -- workflow/task envelope inspection tools
"""
