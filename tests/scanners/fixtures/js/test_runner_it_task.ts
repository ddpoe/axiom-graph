import { it } from 'vitest';
import { task } from 'axiom-annotations';

it('bridge service handles A then B', task({purpose: 'unit-level bridge contract'})(async () => {
  // body intentionally empty for fixture
}));
